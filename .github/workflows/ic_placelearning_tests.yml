name: Run Python tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          python-version: "3.12"
          activate-environment: ic_placelearning
          auto-update-conda: true
          miniforge-variant: Miniforge3
          channels: conda-forge
          conda-remove-defaults: true

      - name: Install IC Place Learning and test dependencies
        run: |
          source "$(conda info --base)/etc/profile.d/conda.sh"
          conda activate ic_placelearning
          pip install -e ".[dev]"

      - name: Debug IC Place Learning import
        run: |
          source "$(conda info --base)/etc/profile.d/conda.sh"
          conda activate ic_placelearning
          python -c "import ic_placelearning; print('ic_placelearning imported from:', ic_placelearning.__file__)"

      - name: Run tests
        run: |
          source "$(conda info --base)/etc/profile.d/conda.sh"
          conda activate ic_placelearning
          pytest --cov --cov-branch --cov-report=term --cov-report=xml --junitxml=junit.xml -o junit_family=legacy

      - name: Upload coverage reports to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v5
        with:
          disable_search: true
          fail_ci_if_error: true
          files: coverage.xml
          verbose: true

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v5
        with:
          disable_search: true
          files: junit.xml
          report_type: test_results
          verbose: true
